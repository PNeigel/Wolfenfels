#pragma once

namespace Buttons {
	enum Buttons
	{
		UP, DOWN, LEFT, RIGHT, SHOOT, STRAFE, WEAPON2, ACTION
	};
}

namespace Shader {
	enum Shader
	{
		COLOR_SCREEN, TEXTURE_PROJ, TEXTURE_SCREEN	
	};
}